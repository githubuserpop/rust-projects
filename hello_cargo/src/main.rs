fn main() {
    println!("Hello, world!");
}
// $ cargo build to build project
// $ cargo run to build and run project
// $ cargo check to check if project compiles - useful for when you need to compile multiple times just to see if code works but don't need exec file
// $ cargo build --release to build project in release mode - slower but faster exec file, compiles w/ optimizations
// $ cargo doc --open to build documentation for project
// $ cargo update to update dependencies
// $ cargo new hello_cargo to create new project
// $ ./target/debug/hello_cargo to run project
